# web-compare
Web Compare is a simple, browser-based tool, designed to visually compare two text files side-by-side. Users can load or paste file contents, highlight differences or similarities, and clear highlights—all within a clean, modern interface. No backend required; everything runs locally in the browser.
